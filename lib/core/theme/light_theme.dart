import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF6D0F1A);
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;

  static Color overlay = Colors.white..withValues(alpha: .90);
  static Color card = Colors.white..withValues(alpha: .90);
}
