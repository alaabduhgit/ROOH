import 'package:flutter/material.dart';

class AppColors {
  /// اللون الرئيسي
  static const Color primary = Color(0xFF6D0F1A);

  /// أبيض
  static const Color white = Colors.white;

  /// أسود
  static const Color black = Colors.black;

  /// رمادي
  static const Color grey = Colors.grey;

  /// خلفية شفافة
  static Color overlay = Colors.white.withOpacity(.08);

  /// لون الكروت
  static Color card = Colors.white.withOpacity(.90);
}
